let transportsName = ["Corolla", "Alto", "Civic"];

for(let i = 0; i < transportsName.length; i++){
    eachTransportationName = transportsName[i];
    document.write(`<br> I would like to own a ${eachTransportationName}.`);
}